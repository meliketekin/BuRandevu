/** Tek satırlı FormInput / CustomSelect varsayılan yüksekliği (label ortalaması buna göre) */
export const DEFAULT_FORM_CONTROL_HEIGHT = 62;

/** multiline FormInput için `height` verilmezken içerik alanı yüksekliği */
export const DEFAULT_FORM_MULTILINE_HEIGHT = 120;
